//#include <cs50.h> this file wont run because c doesn't know what strings are
#include <stdio.h>


int main(void)
{
  printf("name: ", );
  string name = get_string();
  printf("hello, %s\n", name);
}

//
